[phases.install]
cmds = ["pip install fastapi uvicorn langchain langchain-community langchain-text-splitters chromadb sentence-transformers pypdf python-dotenv httpx anthropic"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
